**RU:**
# Habit Tracker Bot

Telegram бот для отслеживания привычек.

## Установка

1. Клонируйте репозиторий
2. Создайте виртуальное окружение
3. Установите зависимости: `pip install -r requirements.txt`
4. Создайте файл `.env` по примеру `.env_EXAMPLE` и добавьте в него свой токен бота
5. Запустите: `python bot.py`

## Использование

TODO...


---

**EN:**
# Habit Tracker Bot

Telegram bot for tracking habits.

## Installation

1. Clone the repository
2. Create a virtual environment
3. Install the dependencies: `pip install -r requirements.txt`
4. Create a `.env` file following the example of `.env_EXAMPLE` and add your bot token to it
5. Run: `python bot.py`

## Using

TODO...
